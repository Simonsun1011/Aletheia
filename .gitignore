# 密钥与环境 —— 永不入库
.env
.env.*
!.env.example
*.pem
serviceAccount*.json

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# Node（Phase 1+）
node_modules/
.next/

# 可再生数据：SQLite库不进git（app.db可由JSONL重建）
*.db
*.db-journal
*.db-wal
*.db-shm
.fuse_hidden*
data/market/

# 隐私数据（DESIGN v0.4，2026-07-12）
# 个人判断/随感/成交（journal JSONL）、买入方案、报告永不进任何 git（含私有仓）
# 判断类数据的异地备份走 Firestore 免费层（独立切片）
data/journal/*.jsonl
data/plans/
data/reports/

# 真相源翻转（2026-07-17）：本仓为私有仓，文档（DESIGN.md/CLAUDE.md/docs//.cursor/）
# 自此入库跟踪获得版本史与异地备份；公开分享唯一通道是 scripts/publish.sh（净化快照）。

# 日志
logs/

# 系统
.DS_Store
